# what is String->

# String is a data type in Python.

# String is sequence of characters enclosed in quotes.

# we can write the string in following ways->

a = "sudhir"
b = 'sudhir'
c = '''sudhir'''

name = "Sudhir"
nameshort = name[0:3] # it gives the string from 0th index to till 3 (not 3).
nameshort = len(name) # it gives the length of the name.

print(nameshort)

character1 = name[3] # it give the single element at 3rd place starting from the 0 index.
print(character1)
